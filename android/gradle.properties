org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableR8.fullMode=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Kotlin's incremental compiler stores plugin source paths relative to this
# project. The project lives on D: while the pub cache is on C:, and Windows
# has no relative path between two drives, so the cache write throws and the
# build fails. Non-incremental compilation sidesteps it.
kotlin.incremental=false
